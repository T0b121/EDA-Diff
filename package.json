{
  "_filePurpose": [
    "Web and WebAssembly toolchain manifest for EDA-Diff.",
    "Defines the TypeScript/Vite commands and the Rust-to-WASM browser build."
  ],
  "name": "eda-diff",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:core": "wasm-pack build src/core --target web --out-dir ../web/generated/core --release",
    "dev": "npm run build:core && vite --config vite.config.ts",
    "build:web": "tsc --noEmit && vite build --config vite.config.ts",
    "build": "npm run build:core && npm run build:web",
    "preview": "vite preview --config vite.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
